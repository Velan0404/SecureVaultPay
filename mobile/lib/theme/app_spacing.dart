/// Shared spacing scale — every screen should reach for these instead of
/// hardcoding padding/gap numbers, so density stays consistent app-wide.
class AppSpacing {
  AppSpacing._();

  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 20;
  static const double xl = 24;
  static const double xxl = 32;
}
